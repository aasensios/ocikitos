export interface Str {
    id: number;
    locus: string;
    chromosome: number;
    start_coordenate: number;
    end_coordenate: number;
    repeat_motif: string;
    min_size: number;
    max_size: number;
    min_repeats: number;
    max_repeats: number;
    annealing_temp: number;
}
